# Environment variables — copy this to .env and fill in your values.
# NEVER commit .env to git.

# ---- Required ----
OPENAI_API_KEY=sk-...your-key-here...

# ---- Optional (defaults shown) ----
DAILY_INVOKE_LIMIT=60
DAILY_COPILOT_LIMIT=60
RISK_WINDOW_SIZE=50
CORS_ORIGINS=*
